(coq.theory
  (name xhl)
  (package xhl)
  (theories
     HB
     elpi
     elpi_elpi
     mathcomp)
  (flags -w -parsing
         -w -projection-no-head-constant
         -w -redundant-canonical-projection
         -w -ambiguous-paths))
(include_subdirs qualified)
